version: '3.8'

services:
  sound-bath:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sound-bath-sanctuary
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
